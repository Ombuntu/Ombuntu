screencopy {
    # Ask which screen or window to share every time instead of remembering the choice
    # (Omarchy's default is true for convenience).
    allow_token_by_default = false
    custom_picker_binary = hyprland-preview-share-picker
}
