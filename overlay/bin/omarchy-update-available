#!/bin/bash

# omarchy:summary=Report whether apt has pending upgrades (exit 0 when updates are available)

count=$(apt-get -s dist-upgrade 2>/dev/null | grep -c '^Inst ')

if (( count > 0 )); then
  echo "$count package update(s) available"
  exit 0
else
  echo "System is up to date"
  exit 1
fi
