#!/bin/bash
set -e
command -v flatpak >/dev/null || sudo apt-get install -y flatpak
flatpak remotes --system 2>/dev/null | grep -q flathub || sudo flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
