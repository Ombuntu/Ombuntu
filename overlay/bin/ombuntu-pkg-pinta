#!/bin/bash
# Pinta from Flathub (Ubuntu 26.04 no longer packages it)
set -e
flatpak info com.github.PintaProject.Pinta >/dev/null 2>&1 && exit 0
ombuntu-pkg-flatpak
sudo flatpak install -y flathub com.github.PintaProject.Pinta
