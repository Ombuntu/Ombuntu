#!/bin/bash
# Crush from Charm's official apt repository
source "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/ombuntu-lib.sh"
set -e
apt_repo_add charm https://repo.charm.sh/apt/gpg.key "deb https://repo.charm.sh/apt/ * *"
sudo apt-get install -y crush
