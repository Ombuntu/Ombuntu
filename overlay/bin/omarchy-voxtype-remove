#!/bin/bash

# omarchy:summary=Remove Voxtype dictation

systemctl --user disable --now voxtype.service 2>/dev/null
rm -f ~/.local/bin/voxtype ~/.local/bin/voxtype-audio-bridge
rm -rf ~/.config/voxtype ~/.local/share/voxtype
omarchy-restart-waybar
echo "Voxtype removed."
