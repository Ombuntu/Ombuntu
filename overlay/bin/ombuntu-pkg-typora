#!/bin/bash
# Typora from its official apt repository
source "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/ombuntu-lib.sh"
set -e
apt_repo_add typora https://typora.io/linux/public-key.asc "deb https://typora.io/linux ./"
sudo apt-get install -y typora
